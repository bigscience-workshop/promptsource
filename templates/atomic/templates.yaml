dataset: atomic
templates:
  23bd81a2-b1f0-42cd-ac51-78624fae94bc: !Template
    id: 23bd81a2-b1f0-42cd-ac51-78624fae94bc
    jinja: 'Answer the following questions when {{event}}:

      {% if xIntent|length>0 %}

      What was PersonX''s intention?

      {% endif %}

      {% if xNeed|length>0 %}

      What did PersonX need?

      {% endif %}

      {% if xAttr|length>0 %}

      How is PersonX seen as?

      {% endif %}

      {% if xReact|length>0 %}

      How does PersonX feel?

      {% endif %}

      {% if xWant|length>0 %}

      What does PersonX want?

      {% endif %}

      {% if xEffect|length>0 %}

      What effect does event have on PersonX?

      {% endif %}

      {% if oReact|length>0 %}

      How do others feel?

      {% endif %}

      {% if oWant|length>0 %}

      What do others want?

      {% endif %}

      {% if oEffect|length>0 %}

      What effect does event have on others?

      {% endif %}

      |||

      {% if xIntent|length>0 %}

      PersonX''s intention was {{xIntent | join(", ") }}

      {% endif %}

      {% if xNeed|length>0 %}

      PersonX needed {{xNeed | join(", ") }}

      {% endif %}

      {% if xAttr|length>0%}

      PersonX is seen as {{xAttr | join(", ") }}

      {% endif %}

      {% if xReact|length>0%}

      PersonX feels {{xReact | join(", ") }}

      {% endif %}

      {% if xWant|length>0 %}

      PersonX wants {{xWant | join(", ") }}

      {% endif %}

      {% if xEffect|length>0 %}

      Effect the event has on PersonX: {{xEffect | join(", ") }}

      {% endif %}

      {% if oReact|length>0 %}

      Others feel {{oReact | join(", ") }}

      {% endif %}

      {% if oWant |length>0%}

      Others want {{oWant | join(", ") }}

      {% endif %}

      {% if oEffect|length>0 %}

      Effect the event has on others: {{oEffect | join(", ") }}

      {% endif %}'
    name: Outcome prefix 2
    reference: ''
  610212dd-da7d-4ad5-aabb-df85649253e4: !Template
    id: 610212dd-da7d-4ad5-aabb-df85649253e4
    jinja: '{{event}}

      {% if xIntent|length>0 %}

      What was PersonX''s intention?

      {% endif %}

      {% if xNeed|length>0 %}

      What did PersonX need?

      {% endif %}

      {% if xAttr|length>0 %}

      How is PersonX seen as?

      {% endif %}

      {% if xReact|length>0 %}

      How does PersonX feel?

      {% endif %}

      {% if xWant|length>0 %}

      What does PersonX want?

      {% endif %}

      {% if xEffect|length>0 %}

      What effect does event have on PersonX?

      {% endif %}

      {% if oReact|length>0 %}

      How do others feel?

      {% endif %}

      {% if oWant|length>0 %}

      What do others want?

      {% endif %}

      {% if oEffect|length>0 %}

      What effect does event have on others?

      {% endif %}

      |||

      {% if xIntent|length>0 %}

      PersonX''s intention was {{xIntent | join(", ") }}

      {% endif %}

      {% if xNeed|length>0 %}

      PersonX needed {{xNeed | join(", ") }}

      {% endif %}

      {% if xAttr|length>0%}

      PersonX is seen as {{xAttr | join(", ") }}

      {% endif %}

      {% if xReact|length>0%}

      PersonX feels {{xReact | join(", ") }}

      {% endif %}

      {% if xWant|length>0 %}

      PersonX wants {{xWant | join(", ") }}

      {% endif %}

      {% if xEffect|length>0 %}

      Effect the event has on PersonX: {{xEffect | join(", ") }}

      {% endif %}

      {% if oReact|length>0 %}

      Others feel {{oReact | join(", ") }}

      {% endif %}

      {% if oWant |length>0%}

      Others want {{oWant | join(", ") }}

      {% endif %}

      {% if oEffect|length>0 %}

      Effect the event has on others: {{oEffect | join(", ") }}

      {% endif %}'
    name: Event + Questions
    reference: https://homes.cs.washington.edu/~msap/atomic/
  a906c27f-a156-472a-b6fc-d00a6721a16a: !Template
    id: a906c27f-a156-472a-b6fc-d00a6721a16a
    jinja: '{% if xIntent|length>0 %}

      What was PersonX''s intention if {{event}}?

      {% endif %}

      {% if xNeed|length>0 %}

      What did PersonX need if {{event}}?

      {% endif %}

      {% if xAttr|length>0 %}

      How is PersonX seen as if {{event}}?

      {% endif %}

      {% if xReact|length>0 %}

      How does PersonX feel if {{event}}?

      {% endif %}

      {% if xWant|length>0 %}

      What does PersonX want if {{event}}?

      {% endif %}

      {% if xEffect|length>0 %}

      What effect does the event have on PersonX if {{event}}?

      {% endif %}

      {% if oReact|length>0 %}

      How do others feel if {{event}}?

      {% endif %}

      {% if oWant|length>0 %}

      What do others want if {{event}}?

      {% endif %}

      {% if oEffect|length>0 %}

      What effect does the event have on others if {{event}}?

      {% endif %}

      |||

      {% if xIntent|length>0 %}

      PersonX''s intention was {{xIntent | join(", ") }}

      {% endif %}

      {% if xNeed|length>0 %}

      PersonX needed {{xNeed | join(", ") }}

      {% endif %}

      {% if xAttr|length>0%}

      PersonX is seen as {{xAttr | join(", ") }}

      {% endif %}

      {% if xReact|length>0%}

      PersonX feels {{xReact | join(", ") }}

      {% endif %}

      {% if xWant|length>0 %}

      PersonX wants {{xWant | join(", ") }}

      {% endif %}

      {% if xEffect|length>0 %}

      Effect the event has on PersonX: {{xEffect | join(", ") }}

      {% endif %}

      {% if oReact|length>0 %}

      Others feel {{oReact | join(", ") }}

      {% endif %}

      {% if oWant |length>0%}

      Others want {{oWant | join(", ") }}

      {% endif %}

      {% if oEffect|length>0 %}

      Effect the event has on others: {{oEffect | join(", ") }}

      {% endif %}'
    name: Questions if Event
    reference: Same as Event + Questions with an additional if followed by the event.
  ac5d9e2a-6968-4418-b22e-4ab2af7d6abd: !Template
    id: ac5d9e2a-6968-4418-b22e-4ab2af7d6abd
    jinja: 'Determine the outcome when {{event}}.

      {% if xIntent|length>0 %}

      What was PersonX''s intention?

      {% endif %}

      {% if xNeed|length>0 %}

      What did PersonX need?

      {% endif %}

      {% if xAttr|length>0 %}

      How is PersonX seen as?

      {% endif %}

      {% if xReact|length>0 %}

      How does PersonX feel?

      {% endif %}

      {% if xWant|length>0 %}

      What does PersonX want?

      {% endif %}

      {% if xEffect|length>0 %}

      What effect does event have on PersonX?

      {% endif %}

      {% if oReact|length>0 %}

      How do others feel?

      {% endif %}

      {% if oWant|length>0 %}

      What do others want?

      {% endif %}

      {% if oEffect|length>0 %}

      What effect does event have on others?

      {% endif %}

      |||

      {% if xIntent|length>0 %}

      PersonX''s intention was {{xIntent | join(", ") }}

      {% endif %}

      {% if xNeed|length>0 %}

      PersonX needed {{xNeed | join(", ") }}

      {% endif %}

      {% if xAttr|length>0%}

      PersonX is seen as {{xAttr | join(", ") }}

      {% endif %}

      {% if xReact|length>0%}

      PersonX feels {{xReact | join(", ") }}

      {% endif %}

      {% if xWant|length>0 %}

      PersonX wants {{xWant | join(", ") }}

      {% endif %}

      {% if xEffect|length>0 %}

      Effect the event has on PersonX: {{xEffect | join(", ") }}

      {% endif %}

      {% if oReact|length>0 %}

      Others feel {{oReact | join(", ") }}

      {% endif %}

      {% if oWant |length>0%}

      Others want {{oWant | join(", ") }}

      {% endif %}

      {% if oEffect|length>0 %}

      Effect the event has on others: {{oEffect | join(", ") }}

      {% endif %}'
    name: Outcome prefix 1
    reference: Same as Event + Questions with a prefix "Determine the outcome"
  c2fd60d0-4fbd-4d08-874d-1585b530ce59: !Template
    id: c2fd60d0-4fbd-4d08-874d-1585b530ce59
    jinja: "Given the following event: {{event}};\nanswer the following questions:\
      \ \n{% if xIntent|length>0 %}\nWhat was PersonX's intention?\n{% endif %}\n\
      {% if xNeed|length>0 %}\nWhat did PersonX need?\n{% endif %}\n{% if xAttr|length>0\
      \ %}\nHow is PersonX seen as?\n{% endif %}\n{% if xReact|length>0 %}\nHow does\
      \ PersonX feel?\n{% endif %}\n{% if xWant|length>0 %}\nWhat does PersonX want?\n\
      {% endif %}\n{% if xEffect|length>0 %}\nWhat effect does event have on PersonX?\n\
      {% endif %}\n{% if oReact|length>0 %}\nHow do others feel?\n{% endif %}\n{%\
      \ if oWant|length>0 %}\nWhat do others want?\n{% endif %}\n{% if oEffect|length>0\
      \ %}\nWhat effect does event have on others?\n{% endif %}\n|||\n{% if xIntent|length>0\
      \ %}\nPersonX's intention was {{xIntent | join(\", \") }}\n{% endif %}\n{% if\
      \ xNeed|length>0 %}\nPersonX needed {{xNeed | join(\", \") }}\n{% endif %}\n\
      {% if xAttr|length>0%}\nPersonX is seen as {{xAttr | join(\", \") }}\n{% endif\
      \ %}\n{% if xReact|length>0%}\nPersonX feels {{xReact | join(\", \") }}\n{%\
      \ endif %}\n{% if xWant|length>0 %}\nPersonX wants {{xWant | join(\", \") }}\n\
      {% endif %}\n{% if xEffect|length>0 %}\nEffect the event has on PersonX: {{xEffect\
      \ | join(\", \") }}\n{% endif %}\n{% if oReact|length>0 %}\nOthers feel {{oReact\
      \ | join(\", \") }}\n{% endif %}\n{% if oWant |length>0%}\nOthers want {{oWant\
      \ | join(\", \") }}\n{% endif %}\n{% if oEffect|length>0 %}\nEffect the event\
      \ has on others: {{oEffect | join(\", \") }}\n{% endif %}"
    name: Given Event + Questions
    reference: 'Same as Event + Questions with a prefix "Given the following event" '
