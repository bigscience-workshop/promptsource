dataset: blimp
subset: complex_NP_island
templates:
  0495b672-0eeb-4486-8121-935a3bdb4a2d: !Template
    answer_choices: A ||| B
    id: 0495b672-0eeb-4486-8121-935a3bdb4a2d
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Read the following two sentences.

      {% if shuffled_order == 0 %}

      Sentence A:  {{ sentence_good }}

      Sentence B: {{ sentence_bad }}

      {% else %}

      Sentence A:  {{ sentence_bad }}

      Sentence B: {{ sentence_good }}

      {% endif %}

      Which one is a better sentence of English, A or B? ||| {% if shuffled_order
      == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: better_A_B
    reference: ''
  0dbb4a55-45cd-46b7-8a0a-78580888d312: !Template
    answer_choices: A ||| B
    id: 0dbb4a55-45cd-46b7-8a0a-78580888d312
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Between the two sentences, which one is grammatical? Please answer A or B.

      {% if shuffled_order == 0 %}

      A: {{ sentence_good }}

      B: {{ sentence_bad }}

      {% else %}

      A: {{ sentence_bad }}

      B: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_between_A_B
    reference: ''
  0fb945e8-2f72-4622-a364-0716fe4c6a17: !Template
    answer_choices: Yes ||| No
    id: 0fb945e8-2f72-4622-a364-0716fe4c6a17
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a bad sentence of English, No or Yes? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[1] }} {% else %} {{ answer_choices[0] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_bad_no_yes
    reference: ''
  296e0a9a-9eff-4ee9-9740-a22065d2b097: !Template
    answer_choices: 1 ||| 2
    id: 296e0a9a-9eff-4ee9-9740-a22065d2b097
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Between the two sentences, which one is grammatical? Please answer 1 or 2.

      {% if shuffled_order == 0 %}

      1: {{ sentence_good }}

      2: {{ sentence_bad }}

      {% else %}

      1: {{ sentence_bad }}

      2: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_between_1_2
    reference: ''
  730ab874-f5d2-4527-9a7b-41f9474f6de1: !Template
    answer_choices: Yes ||| No
    id: 730ab874-f5d2-4527-9a7b-41f9474f6de1
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a good sentence of English, Yes or No? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[0] }} {% else %} {{ answer_choices[1] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_good_yes_no
    reference: ''
  7beba882-e2e7-4f13-9599-f156d83b819c: !Template
    answer_choices: A ||| B
    id: 7beba882-e2e7-4f13-9599-f156d83b819c
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Read the following two sentences.

      {% if shuffled_order == 0 %}

      Sentence A:  {{ sentence_good }}

      Sentence B: {{ sentence_bad }}

      {% else %}

      Sentence A:  {{ sentence_bad }}

      Sentence B: {{ sentence_good }}

      {% endif %}

      Which one is a better sentence of English, B or A? ||| {% if shuffled_order
      == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: better_B_A
    reference: ''
  8798f8e4-7b90-435e-9630-fbfdb187ee46: !Template
    answer_choices: A ||| B
    id: 8798f8e4-7b90-435e-9630-fbfdb187ee46
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Which one of the following sentences is grammatical? Please answer A or B.

      {% if shuffled_order == 0 %}

      A: {{ sentence_good }}

      B: {{ sentence_bad }}

      {% else %}

      A: {{ sentence_bad }}

      B: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_which_one_A_B
    reference: ''
  b77195ba-a7e0-4225-a343-6f0239621f04: !Template
    answer_choices: Yes ||| No
    id: b77195ba-a7e0-4225-a343-6f0239621f04
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a bad sentence of English, Yes or No? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[1] }} {% else %} {{ answer_choices[0] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_bad_yes_no
    reference: ''
  f09f244c-048a-4591-aee2-bff934b44cdd: !Template
    answer_choices: Yes ||| No
    id: f09f244c-048a-4591-aee2-bff934b44cdd
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a good sentence of English, No or Yes? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[0] }} {% else %} {{ answer_choices[1] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_good_no_yes
    reference: ''
  f26a0feb-2759-4166-bb10-67337ca716a0: !Template
    answer_choices: 1 ||| 2
    id: f26a0feb-2759-4166-bb10-67337ca716a0
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Which one of the following sentences is grammatical? Please answer 1 or 2.

      {% if shuffled_order == 0 %}

      1: {{ sentence_good }}

      2: {{ sentence_bad }}

      {% else %}

      1: {{ sentence_bad }}

      2: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_which_one_1_2
    reference: ''
