dataset: blimp
subset: wh_questions_subject_gap_long_distance
templates:
  28ebe131-6988-4bb0-a157-a0f7accef5c8: !Template
    answer_choices: Yes ||| No
    id: 28ebe131-6988-4bb0-a157-a0f7accef5c8
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a good sentence of English, Yes or No? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[0] }} {% else %} {{ answer_choices[1] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_good_yes_no
    reference: ''
  4eaf3a66-94d5-4e78-a0e0-fbf73ac9fccd: !Template
    answer_choices: Yes ||| No
    id: 4eaf3a66-94d5-4e78-a0e0-fbf73ac9fccd
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a good sentence of English, No or Yes? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[0] }} {% else %} {{ answer_choices[1] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_good_no_yes
    reference: ''
  4eba01cc-3f89-4ee2-9847-b4078b71a224: !Template
    answer_choices: 1 ||| 2
    id: 4eba01cc-3f89-4ee2-9847-b4078b71a224
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Between the two sentences, which one is grammatical? Please answer 1 or 2.

      {% if shuffled_order == 0 %}

      1: {{ sentence_good }}

      2: {{ sentence_bad }}

      {% else %}

      1: {{ sentence_bad }}

      2: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_between_1_2
    reference: ''
  5b04b4a2-11ad-4976-8e7c-dae6f15651a8: !Template
    answer_choices: 1 ||| 2
    id: 5b04b4a2-11ad-4976-8e7c-dae6f15651a8
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Which one of the following sentences is grammatical? Please answer 1 or 2.

      {% if shuffled_order == 0 %}

      1: {{ sentence_good }}

      2: {{ sentence_bad }}

      {% else %}

      1: {{ sentence_bad }}

      2: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_which_one_1_2
    reference: ''
  8ce39c09-68b5-4356-9a5d-50f698776bdf: !Template
    answer_choices: A ||| B
    id: 8ce39c09-68b5-4356-9a5d-50f698776bdf
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Which one of the following sentences is grammatical? Please answer A or B.

      {% if shuffled_order == 0 %}

      A: {{ sentence_good }}

      B: {{ sentence_bad }}

      {% else %}

      A: {{ sentence_bad }}

      B: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_which_one_A_B
    reference: ''
  b6f677a8-d25b-4c30-a818-9e7abe2edca8: !Template
    answer_choices: A ||| B
    id: b6f677a8-d25b-4c30-a818-9e7abe2edca8
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Read the following two sentences.

      {% if shuffled_order == 0 %}

      Sentence A:  {{ sentence_good }}

      Sentence B: {{ sentence_bad }}

      {% else %}

      Sentence A:  {{ sentence_bad }}

      Sentence B: {{ sentence_good }}

      {% endif %}

      Which one is a better sentence of English, B or A? ||| {% if shuffled_order
      == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: better_B_A
    reference: ''
  c7607b5c-2383-41de-8e05-5a3975d5be2d: !Template
    answer_choices: Yes ||| No
    id: c7607b5c-2383-41de-8e05-5a3975d5be2d
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a bad sentence of English, No or Yes? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[1] }} {% else %} {{ answer_choices[0] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_bad_no_yes
    reference: ''
  d385fbf9-6bbb-4d9f-96e3-b3e039d6679d: !Template
    answer_choices: A ||| B
    id: d385fbf9-6bbb-4d9f-96e3-b3e039d6679d
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Between the two sentences, which one is grammatical? Please answer A or B.

      {% if shuffled_order == 0 %}

      A: {{ sentence_good }}

      B: {{ sentence_bad }}

      {% else %}

      A: {{ sentence_bad }}

      B: {{ sentence_good }}

      {% endif %}

      |||

      {% if shuffled_order == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: grammatical_between_A_B
    reference: ''
  ecab1d1c-7f5a-488b-b2a9-f8d233c1d832: !Template
    answer_choices: A ||| B
    id: ecab1d1c-7f5a-488b-b2a9-f8d233c1d832
    jinja: '{% set shuffled_order = [0, 1] | random %}

      Read the following two sentences.

      {% if shuffled_order == 0 %}

      Sentence A:  {{ sentence_good }}

      Sentence B: {{ sentence_bad }}

      {% else %}

      Sentence A:  {{ sentence_bad }}

      Sentence B: {{ sentence_good }}

      {% endif %}

      Which one is a better sentence of English, A or B? ||| {% if shuffled_order
      == 0 %}

      {{ answer_choices[0] }}

      {% else %}

      {{ answer_choices[1] }}

      {% endif %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: true
    name: better_A_B
    reference: ''
  f477c93b-9cd6-4504-8dc4-0e9e871a8c8c: !Template
    answer_choices: Yes ||| No
    id: f477c93b-9cd6-4504-8dc4-0e9e871a8c8c
    jinja: '"{% set shuffled_order = [0, 1] | random %}{% if shuffled_order == 0 %}{{
      sentence_good }}{% else %}{{ sentence_bad }}{% endif %}"

      Is this sentence a bad sentence of English, Yes or No? ||| {% if shuffled_order
      == 0 %} {{ answer_choices[1] }} {% else %} {{ answer_choices[0] }} {% endif
      %}'
    metadata: !TemplateMetadata
      choices_in_prompt: true
      metrics:
      - Accuracy
      original_task: false
    name: single_sentence_bad_yes_no
    reference: ''
